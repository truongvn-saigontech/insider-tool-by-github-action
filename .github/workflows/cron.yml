name: "Close stale issues"
on:
  schedule:
    - cron: "* * * * *"

jobs:
  build:
    name: Trigger Site Rebuild
    runs-on: ubuntu-latest
    steps:
      - name: cURL request
        run: echo "hello world"
